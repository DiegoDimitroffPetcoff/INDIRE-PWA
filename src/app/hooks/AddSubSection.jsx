/* import SubSection from "../components/Projects/Project/Section/SubSection/SubSection";


const addSubSection = async (counter, id, setAdditionalSections) => {
    let result = await counter(id + 1);
    setAdditionalSections([
      ...additionalSections,
      <SubSection
        key={additionalSections.length}
        id={id}
        subSection={subSection}
        setSubSections={setSubSections}
        subProjectCounts={result}
        setSubProjectCounts={setSubProjectCounts}
      />,
    ]);
  }; */